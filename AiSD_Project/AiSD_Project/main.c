#include <stdio.h>
#include <stdlib.h>
#include "sort_project.h"

int main()
{
	system("CHCP 1250 >NUL");
	/*int min = 0;
	int max = 10;
	int N = 10000000;

	//char name[50] = "ststs";

	 
	int* array = calloc(N, sizeof(int));
//fill_array_reverse(array, N);
	fill_array_rand(array, N, 0, 100);
save_array("tabela", array, N);
shell_sort(array, N);
save_array("tabela_soreted", array, N);




	//print_array_a(array, N,-1);


	//shell_sort (array, N);
	//insert_sort(array, N);
	//printf("\n");
	//print_array(array, N);
	//generarate_times(100000, 1000);
	//int proces_insert = insert_sort_calc(array, N);
	//	fill_array_reverse(array, N);
		//int proces_shell = shell_sort_calc(array, N);
	//shell_sort_step(array, N);
	//insert_sort_step(array, N);
		//printf("insert:%d\nshell:%d", proces_insert, proces_shell);
	printf("\n\n\n");
	*/
	start_gui();
}