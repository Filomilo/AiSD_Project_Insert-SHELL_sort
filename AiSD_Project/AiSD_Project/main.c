#include <stdio.h>
#include <stdlib.h>
#include "sort_project.h"

int main()
{
	int min = 0;
	int max = 10;
	int N = 100000;

	 
	 
	
//	fill_array_reverse(array, N);
	//print_array(array, N);


	//shell_sort (array, N);
	//insert_sort(array, N);
	//printf("\n");
	//print_array(array, N);
	generarate_times(100000, 1000);
	printf("\n\n\n");
}